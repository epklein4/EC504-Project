    /////////////////////////////////////////////////////
    //Ethan, you can help here.
    //I'm trying to get it to display circles on the image where the user clicks.
    //Right now, only one circle shows up
    //The commented section here should be pretty close

    /*namedWindow( "newDisplay" );

    Mat newIm = resizedIm;

    int circleX, circleY;
    Point centerB, centerR;

    int radius = 5;
    int thickness = 2;

    for (int i = 0; i < nSource; i++) {
        circleX = (bgfgNull[0][i]).x;
        circleY = (bgfgNull[0][i]).y;
        centerB.x = circleX;
        centerB.y = circleY;
        circle(newIm, centerB, radius, Scalar(255, 0, 0), thickness);
        }

    for (int i = 0; i < nSink; i++) {
        circleX = (bgfgNull[0][i]).x;
        circleY = (bgfgNull[0][i]).y;
        centerR.x = circleX;
        centerR.y = circleY;
        circle(newIm, centerB, radius, Scalar(0, 0, 255), thickness);
        }

    imshow( "newDisplay", newIm ); // show image
    waitKey(0);*/
